export type Game = {
    id: number
    name: string
    description: string
    requirement: string
    types: string
    createdAt: string
    startDate: string
    endDate: string
    imageURL: string
    city: string
    address: string
    price: string
}