export type Competition = {
    id: number
    title: string
    description: string
    types:string
    shortDescription: string
    createdAt: string
    startDate: string
    endDate: string
    imageURL: string
    city: string
    address: string
    placeName: string
    entryFee: string
    maxParticipants: number
    currentParticipants: number
    requirements: string
    minAge: number
    maxAge: number
    status: string
    format: string
    price:string
}